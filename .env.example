# Backend API base URL (used for session/start and other API calls)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Vapi public key for the web SDK (client-side). Get from dashboard.vapi.ai
NEXT_PUBLIC_VAPI_PUBLIC_KEY=your-vapi-public-key
